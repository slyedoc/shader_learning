use bevy::prelude::*;

pub struct ShaderPlugin;

impl Plugin for ShaderPlugin {
    fn build(&self, app: &mut App) {
        // app.add_plugin(ShaderTimePlugin)
        //     .add_plugin(RandomMaterialPlugin);
    }
}

